import styled from "styled-components"

export const ProjectsSectionStyled = styled.section`
  min-height: 100vh;

  background: linear-gradient(0deg, var(--color-brand-3), var(--color-brand-2));

  & > div > h1 {
    padding-top: 32px;
  }
`
